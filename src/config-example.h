#ifndef CONFIG_H
#define CONFIG_H

#define HUE_USER_ID "id" // The user id for your philips hue bridge that needs to generated
#define HUE_IP "ip" // The ip for philips hue
#define PORT 80 // Default port is 80 for philips hue bridge
#define LIGHT 1

#define WIFI_SSID "ssid" // Name of wifi
#define WIFIF_PASS "secret-password" // Wifi password

#endif
